# 3D Craft app icon

Selected direction: Spark Cube. Prepared with the built-in image-generation tool from the approved logo sheet.

Final prompt: Preserve the approved mint, emerald and lavender cube with a white four-point sparkle. Center the emblem alone on an opaque pale mint square with subtle lavender light; no text, mockup, outer rounded tile or transparency.

The original is `spark-cube-app-icon-original.png`. The iOS asset catalog contains the 1024-pixel delivery asset. Xcode compiles device-specific icon sizes. iOS applies the rounded mask.

Verified: signed device build succeeded; compiled Info.plist names AppIcon as the primary icon; exported IPA and installed/ launched on the paired physical iPhone. Export: `ios/.build-device/exports/3DCraft-Spark-Cube-2026-09-12.ipa`.
